class Config:
    def __init__(self, host, token, port, ssl, driver, model):
        self.host = host
        self.token = token
        self.port = port
        self.ssl = ssl
        self.driver = driver
        self.model = model